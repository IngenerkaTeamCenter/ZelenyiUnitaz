#include "TXLib.h"
#include <iostream>

#include <fstream>
#include <string>

using namespace std;

string get_string_part(string stroka)
{
    int posicia
    string 
        int pos1 = stroka_s_kartinkoi.find(',');
        string adress = stroka_s_kartinkoi.substr(0, pos1);
        stroka_s_kartinkoi = stroka_s_kartinkoi.substr(pos1 + 2);
}

int main()
{
    ifstream map_file;
    map_file.open("Files\\Map.txt");

    txCreateWindow(800, 600);
    while (map_file.good()) {
        string stroka_s_kartinkoi;
        getline (map_file, stroka_s_kartinkoi);


        int pos2 = stroka_s_kartinkoi.find(',');
        string x = stroka_s_kartinkoi.substr(0, pos2);
        stroka_s_kartinkoi = stroka_s_kartinkoi.substr(pos2 + 2);

        int pos3 = stroka_s_kartinkoi.find(',');
        string y = stroka_s_kartinkoi.substr(0, pos3);
        stroka_s_kartinkoi = stroka_s_kartinkoi.substr(pos3 + 2);

        HDC pic = txLoadImage(adress.c_str());
        txBitBlt (txDC(), atoi(x.c_str()), atoi(y.c_str()), 30, 30, pic, 0, 0);
        txDeleteDC(pic);
    }
    map_file.close();

}
